<div class="form__group field">
    <input
       type="{{type}}"
       class="form__field"
       placeholder="{{label}}"
       name="{{name}}"
       id="{{name}}"
       {{required}}
    />

    <label for={{name}} class="form__label">{{label}}</label>
</div>
